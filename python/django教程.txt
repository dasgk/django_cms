1.  pip安装django
2.  pip  install  django -i  https://pypi.doubanio.com/simple/
3.  安装mysqldb
在 https://www.lfd.uci.edu/~gohlke/pythonlibs/#mysqlclient 下载对应的mysqldb库
4. django-admin startproject mysite生成实例网站   （注意不是django-admin.py）
5. cd mysite 
6. python manage.py runserver
7. 访问 http://127.0.0.1:8000/ 即可看到效果

8. include指令是从 from django.urls import path,include  这里面引入的
path('test/', include("mysite.routes.web_test")),

9. 如果path的正则表达式不行的话，试试re_path
re_path(r'show_time/([0-9]{4})', show_time_n)

python的标签：  ifequal
{% ifequal user currentuser %}
 <h1>Welcome!</h1>
{% endifequal %}

标签 for循环
{% for item in todo_list %}
 <p>{{ forloop.counter }}: {{ item }}</p>
{% endfor %}

if标签
{% if athlete_list %}
 <p> dasda</p>
{% else %}
 <p>There are no athletes. Only computer programmers.</p>
{% endif %}


过滤器
{{ name|lower }}

{{ pub_date|date:"F j, Y" }}

使用模板进行页面渲染
from django.template.loader import get_template
from django.template import Context
from django.http import HttpResponse
import datetime
def current_datetime(request):
	now = datetime.datetime.now()
	t = get_template('current_datetime.html')
	html = t.render(Context({'current_date': now}))
	return HttpResponse(html)
	
locals() 的值，它囊括了函数执行到该时间点时所定义的一切变量。 因此，我们将 now 变量重命名为 current_date ，因为那才是模板所预期的变量名称。
在本例中， locals() 并没有带来多 大 的改进，但是如果有多个模板变量要界定而你又想偷懒，这种技术可以减少一些键盘输入。



在页面中嵌入
{% include 'nav.html' %}


求解数组长度 
len(query_set)

获得对象列表 
Exhibit.objects.all()
